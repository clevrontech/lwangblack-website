# Production admin: call API directly (avoids Vercel /api proxy edge cases).
# Override in Vercel → Environment Variables → VITE_API_URL if your API host differs.
VITE_API_URL=https://api.lwangblack.co/api
