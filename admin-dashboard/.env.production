# Production build: scripts/build.js sets VITE_API_URL from LWB_API_BASE or VITE_API_URL (e.g. https://xxx.onrender.com).
# Fallback same-origin /api only works if /api is proxied to the backend (not used for Vercel static + Render API).
VITE_API_URL=/api
